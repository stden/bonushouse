# -*- coding: utf-8 -*-
from django.conf.urls import patterns, url
urlpatterns = patterns('',
    #url(r'^$', 'auctions.views.index', name='auctions_index'),
    #url(r'^bid/(?P<auction_id>\d+)/$', 'auctions.views.add_bid_form', name='auctions_bid'),
    #url(r'^additional-info/(?P<auction_id>\d+)/$', 'auctions.views.additional_info', name='auctions_additional_info'),
)